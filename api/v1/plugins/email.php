<?php
if (defined("mailgun_token") || defined("LIGHTWEB_SENDGRID_API_KEY")) {
    if (!mailgun_token == null || LIGHTWEB_SENDGRID_API_KEY) {
        if (strlen(mailgun_token)) {
            // MailGun Function
        }
    }
}